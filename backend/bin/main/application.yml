spring:
  profiles:
    active:
      - local
    group:
      local:
        - oauth
        - db-local
        - cloud
        - redis
        - python
      dev:
        - oauth
        - db-dev
        - cloud
        - redis
        - python
    include: oauth, db, cloud, redis, python
