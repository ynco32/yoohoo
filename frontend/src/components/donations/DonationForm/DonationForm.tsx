type DonationFormProps = {
  initialShelterId?: number;
  initialDogId?: number;
};

export default function DonationForm({
  initialShelterId,
  initialDogId,
}: DonationFormProps) {
  return (
  <form></form>
  );
}
